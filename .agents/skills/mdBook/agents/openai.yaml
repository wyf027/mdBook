interface:
  display_name: "MdBook"
  short_description: "Repo-specific patterns and workflows for mdBook"
  default_prompt: "Use the mdBook repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true